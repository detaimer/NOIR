# 검증 리포트 — M0~M8 @ 66e7714

- 일시: 2026-09-11 · 브랜치: feat/mvp-implementation · 기준: 커밋 `66e7714` "feat(runner+cli): M8 config_loader + runner + `rt run` (첫 usable 정적 e2e)"
- 커밋 안 된 변경: 없음 (untracked `.claude/skills/validate/`, `.claude/worktrees/` 만 — 구간 무관)
- 종합: ✅ 22 · 🟡 1 · ❌ 0 · 🔀 0 · ⏭ 0 → **M0~M8 계획대로 구현·검증됨. CLI의 실제 실행 경로 미검증 1건(경미)만 보완 권장.**

## 판정 요약
| task | 대상 | 판정 | 핵심 사유 |
|---|---|---|---|
| 1 | pyproject deps + `live` marker + addopts | ✅ PASS | openai/pyyaml/jinja2 + marker + `-m 'not live'` 모두 존재 |
| 2 | tests/conftest.py 공용 fake | ✅ PASS | FakeAdapter(scripted)/FakeJudge/sample Behavior + 팩토리 |
| 3 | core/records.py | ✅ PASS | 6 레코드 frozen·동등·기본값, Attempt 필드 계획과 일치 |
| 4 | core/errors.py | ✅ PASS | 4 예외 + RedteamError base, 필드·메시지 검증 |
| 5 | core/interfaces.py | ✅ PASS | Adapter Protocol(runtime_checkable)·Detector/Probe ABC·ProbeContext |
| 6 | core/budget.py | ✅ PASS | N회 OK / N+1 BudgetExceeded / counter 반영, 초과 미전달 |
| 7 | core/config_schema.py | ✅ PASS | from_dict 순수, self-judge→ConfigError(단 allow_self_judge) |
| 8 | adapters/http_openai.py | ✅ PASS | Message→payload, generate/chat, 오류→AdapterError, 네트워크 X |
| 9 | adapters/manual_io.py | ✅ PASS | scripted io, 멀티턴, sentinel/EOF 종료 + ADAPTERS 등록 |
| 10 | detectors/refusal_match.py | ✅ PASS | 거부→False / 응낙→True, 대소문자·한국어 |
| 11 | detectors/llama_guard.py | ✅ PASS | safe/unsafe 분기 파싱 + 카테고리 추출·맵 |
| 12 | detectors/strong_reject.py | ✅ PASS | 루브릭→0~1 점수, success=score≥τ + DETECTORS 등록 |
| 13 | probes/transforms.py | ✅ PASS | past_tense/base64(왕복)/flip/many_shot 순수·고정출력 |
| 14 | probes/static_probe.py | ✅ PASS | transform 적용, target_calls==1, detection 임베드 + PROBES 등록 |
| 15 | registry.py | ✅ PASS | 이름 해석, unknown→UnknownComponent(추천), available() |
| 16 | behaviors/builtin_seed.py | ✅ PASS | 14건 id유일·전부 domain+tags, ot_ics+attack-ics, 다중 taxonomy |
| 17 | behaviors/loaders.py | ✅ PASS | builtin/jsonl 디스패치, domain·limit 필터, unknown→ConfigError |
| 18 | reporting/aggregate.py | ✅ PASS | ASR/efficiency/SR평균/도메인·카테고리/top3/합치도 정확 수치 |
| 19 | reporting/jsonl_writer.py | ✅ PASS | 3파일 생성·왕복, 비밀키 redact(+원본 불변·중첩) |
| 20 | reporting/terminal_table.py | ✅ PASS | ASCII 표: 기법행/도메인/카테고리 라인 |
| 21 | config_loader.py | ✅ PASS | yaml+dotted 오버라이드+from_dict, api_key_env 해석, target≠judge |
| 22 | runner.py | ✅ PASS | 정적 e2e(전 fake), counter 권위 target_calls, 나머지 judge 채점, tmp 출력 |
| 23 | cli.py `rt run` | 🟡 PARTIAL | 명령 wired·help·dry-run·config오류 검증 O — **실제 실행(runner 호출+출력) 경로 미검증** |

## 자동 게이트
| 게이트 | 결과 | 비고 |
|---|---|---|
| pytest | 157 passed, exit=0 | live 제외, skip 0 |
| 조용한 skip | 없음 | 구간 모든 모듈 실제 import·assert 수행 (거짓 green 없음) |
| live 마커 | 정상 | tests 내 `mark.live` 없음 |
| ruff check · format | clean | 66 files formatted, 위반 0 |
| import-linter | 미설정 (M14) | grep 레이어 검사로 대체 → 위반 없음 |
| basename · 테스트 짝 | OK | 중복 basename 없음, 모든 모듈에 test_<name>.py |
| 비밀키 · Non-goal 의존성 | 없음 | 인라인 키 0, litellm/rich 미도입 |
| deptry | ⚪ 참고 (구간 밖) | jinja2(M12)·pytest/ruff/import-linter/deptry(dev그룹 미설정, M14) DEP002 — 모두 예상됨 |

## task 별 상세

### task 1~2 — M0 설정/공용 fake : ✅ PASS
- `pyproject.toml:11-16` deps(typer/openai/pyyaml/jinja2), `:34` `addopts="-q -m 'not live'"`, `:35-37` `live` marker. 기존 스모크 `test_smoke.py` green.
- `tests/conftest.py` FakeAdapter(None/str/list 시나리오)·FakeJudge(success_on_call)·make_attempt·sample_behavior(s). suite green.

### task 3~7 — M1 core 기반 : ✅ PASS
- records: `Role/Message/Behavior/Turn/DetectionResult/Attempt` 모두 frozen. Attempt 필드(`target_calls·attacker_calls·judge_calls·budget_exhausted·error`)가 plan "핵심 아키텍처"와 일치 — `test_records.py::test_attempt_minimal_and_defaults`.
- errors: 4예외 issubclass(RedteamError), `BudgetExceeded.limit`, `UnknownComponent.available` — `test_errors.py`.
- interfaces: ABC 인스턴스화 TypeError·Protocol isinstance — `test_interfaces.py::test_detector_abc_cannot_instantiate`, `::test_adapter_is_runtime_checkable_protocol`.
- budget: N+1 경계 정확(`budget.py:38` `count >= limit`), 초과 시 inner 미호출 — `test_budget.py::test_n_calls_ok_then_exceeds`.
- config_schema: self-judge `identity()` 동일 → ConfigError, flag 시 허용 — `test_config_schema.py::test_self_judge_same_endpoint_raises`, `::test_self_judge_allowed_with_flag`.

### task 8~9 — M2 adapters : ✅ PASS
- http_openai: client 주입으로 네트워크 없이 payload 매핑·AdapterError 래핑 검증. `adapters/__init__.py:9-12` `ADAPTERS={http_openai, manual}` 등록.
- manual_io: sentinel(`.`)/EOF 종료, 멀티턴 순서 — `test_manual_io.py::test_chat_multi_turn_returns_responses_in_order`.

### task 10~12 — M3 detectors : ✅ PASS
- refusal_match(무모델), llama_guard(safe/unsafe+Sxx), strong_reject(`score=(1-refused)×(spec_norm+conv_norm)/2`, 1~5→0~1 정규화). 임계 경계 0.5==τ→success — `test_strong_reject.py::test_mid_case_exactly_at_threshold_is_success`.
- `detectors/__init__.py:13-17` DETECTORS 팩토리 규약(judge_client 주입/무모델은 무시) — `test_detectors_registry.py`.

### task 13~14 — M4 정적 probe : ✅ PASS
- transforms 순수·결정론·base64 왕복·flip involution — `test_transforms.py`. reverse_text는 flip_attack 구성요소로 export·테스트됨(스코프 내).
- static_probe: DI ctx로 transform→target 1회→judge 임베드, `target_calls==1`·`judge_calls==1` — `test_static_probe.py`. `probes/__init__.py:18-23` PROBES(past_tense/base64/flip_attack/many_shot) 등록.

### task 15 — M5 registry : ✅ PASS
- registry가 roles를 import(역방향 X), 매 호출 새 인스턴스, unknown→추천 목록 — `test_registry.py`.

### task 16~17 — M6 behaviors : ✅ PASS
- builtin 14건(테스트 허용 12~16), 전부 domain+subcat+tags, ot_ics+`attack-ics:*`, taxonomy 3종↑ — `test_builtin_seed.py`. 내용 추상 paraphrase.
- loaders: builtin/`.jsonl` 디스패치, jbb/harmbench는 ConfigError(M13 예정, plan과 일치), domain/limit 필터, 태그 tuple화 — `test_loaders.py`.

### task 18~20 — M7 reporting : ✅ PASS
- aggregate 순수 집계 전 축 정확 수치(overall/per_technique/per_domain/per_category/vulnerable_top3/judge_agreement) — `test_aggregate.py`.
- jsonl_writer: 3파일 왕복 + `_redact`가 `api_key`→`***`·`api_key_env`(이름) 보존·case-insensitive·중첩·원본 불변 — `test_jsonl_writer.py`.
- terminal_table: 헤더/기법행/도메인/카테고리 라인 — `test_terminal_table.py`.

### task 21~22 — M8 config_loader/runner : ✅ PASS
- config_loader: dotted 오버라이드·env 해석(미설정→ConfigError)·self-judge yaml — `test_config_loader.py`.
- runner: behavior마다 새 `BudgetedTarget`, `_run_one`이 `target_calls=counter.count`로 **counter 권위** 기록(Fair-ASR 자가보고 X), `_score_extra_judges`가 primary 외 판정 추가, AdapterError→Attempt.error — `test_runner.py::test_static_e2e_all_fake`, `::test_remaining_judges_score_final_response`, `::test_adapter_error_becomes_attempt_error`.
- ⚪ 참고: `_run_one`의 `BudgetExceeded`→Attempt.error 경로는 러너 레벨 테스트 없음. 정적 tier는 1회 호출뿐이라 도달 불가 — PAIR/Crescendo(M9/M10) test_runner 확장에서 다룰 사항.

### task 23 — M8 CLI `rt run` : 🟡 PARTIAL
- 완료 조건 대조
  - [x] `run` 명령 help 노출 — `test_cli.py::test_run_command_listed_in_help`
  - [x] dry-run 계획 출력·오버라이드 반영 — `::test_run_dry_run_prints_plan_without_executing`, `::test_run_dry_run_applies_override`
  - [x] config 오류 시 비정상 종료 — `::test_run_config_error_exits_nonzero`
  - [ ] **실제 실행 경로**(`runner.run` 호출 → `result.table`·`out_dir` 출력, `--out` 전달) — 대응 테스트 없음. 모든 CLI 테스트가 `--dry-run` 또는 오류 종료라 `cli.py:96-98` 미실행.
- 스펙 일치: plan은 "(주입 runner/--dry-run)"을 제시했으나 구현은 모듈 레벨 `runner.run`을 직접 호출(주입 X). dry-run 경로는 테스트되므로 두 대안 중 하나는 충족. 다만 주입식이 아니라 non-dry 경로를 stub 없이 테스트하기 어려운 구조.
- 발견
  - 🟡 `cli.py:96-98` 실제 실행+출력 경로 미검증 — runner는 `test_runner`에서 독립 e2e 검증되어 리스크는 낮으나, CLI→runner 연결(snapshot·out_root 전달, table echo)은 어떤 테스트도 거치지 않음. `src/redteam/cli.py:96`
  - ⚪ `cli.py:86`가 `load_behaviors`를 계획 출력용으로 1회, `runner.run`이 다시 1회 호출 — 무해한 중복.

## 계획과의 차이
- task 23 "주입 runner": 미구현(모듈 레벨 호출). 근거 문서화 없음 → 의도된 변경이면 plan.md의 "(주입 runner/--dry-run)"를 실제 방식으로 갱신 권장. 아니면 아래 수정 요청대로 실행 경로 테스트 추가.

## 구현 세션 전달용 수정 요청
> 아래 블록을 구현 세션에 그대로 붙여넣으면 된다.

```
M0~M8 검증 리포트(references/validation/2026-09-11-M0-M8-66e7714.md) 기준으로 아래를 보완해줘.
1. 🟡 tests/test_cli.py — `rt run`의 실제 실행 경로(dry-run 아님) 테스트 1개 추가.
   (plan task23 완료 조건: "명령 wired") 방법 택1:
   (a) runner.run 을 monkeypatch 로 스텁해 cli.run 이 snapshot=raw·out_root=out 을
       넘기고 result.table 을 echo 하는지 확인, 또는
   (b) cli.py 의 runner.run 을 주입 가능하게(예: 인자/의존성) 리팩터해 plan 의
       "(주입 runner)" 의도대로 맞추고 fake 로 e2e.
   그리고 manual 타깃 실경로가 input() 로 블로킹되지 않게 fake target 경유로만 테스트할 것.
수정 뒤 `python -m pytest -q -rs` 에서 구간 모듈의 skip 이 0 인지도 확인해줘.
```
