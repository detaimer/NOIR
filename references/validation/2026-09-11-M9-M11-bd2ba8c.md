# 검증 리포트 — M9~M11 @ bd2ba8c

- 일시: 2026-09-11 15:46 · 브랜치: feat/mvp-implementation · 기준: 커밋 `bd2ba8c` "bug fix: 따옴표"
- 커밋 안 된 변경: **구간 관련 파일이 working tree 에 미커밋 상태** — 아래 파일은 마지막 커밋에 없고 현재
  작업본을 그대로(=working tree) 검증했다(이 구간은 아직 커밋 전이라 커밋 사본만으로는 판정 불가):
  - untracked: `src/redteam/probes/pair_probe.py`, `src/redteam/probes/crescendo_probe.py`,
    `tests/test_pair_probe.py`, `tests/test_crescendo_probe.py`
  - modified: `docs/plan.md`, `examples/run.yaml`, `src/redteam/probes/__init__.py`,
    `tests/test_runner.py`, `tests/test_aggregate.py`, `tests/test_static_probe.py`,
    `tests/test_terminal_table.py`
  - 즉 이 리포트는 **커밋이 아니라 현재 작업본** 기준이다(사용자가 "작업 중인 것"을 검증 대상으로 하는 구간).
- 종합: ✅ 3 · 🟡 0 · ❌ 0 · 🔀 0 · ⏭ 0 → **M9~M11 계획대로 구현·검증됨. 완료 조건 전 항목이 의미 있는
  테스트로 커버되고 스펙·설계원칙 위반 없음. (권고: 이 구간을 커밋할 것 — 현재 미커밋)**

## 판정 요약
| task | 대상 | 판정 | 핵심 사유 |
|---|---|---|---|
| 24 (M9) | `probes/pair_probe.py` | ✅ PASS | 성공중단·target/attacker/judge_calls==k·≤B·BudgetExceeded 전파·attacker부재 AdapterError, `"pair"` 등록 + runner e2e 전부 테스트 |
| 25 (M10) | `probes/crescendo_probe.py` | ✅ PASS | conv 성장(길이 1,3,5)·턴별 judge·성공/max_turns 중단·target_calls==turns, `"crescendo"` 등록 + ManualAdapter 멀티턴 e2e + runner budget<max_turns 흡수 |
| 26 (M11) | `test_aggregate`/`test_terminal_table` 확장 | ✅ PASS | 동일-B에서 정적/PAIR/Crescendo avg_calls 분리·ASR·efficiency 랭킹 검증 (구현 로직은 M7 기존, 테스트만 확장 — task 문구와 일치) |

## 자동 게이트
| 게이트 | 결과 | 비고 |
|---|---|---|
| pytest | **184 passed, exit=0** | live 제외, skip 0 |
| 조용한 skip | 없음 | 구간 4개 신규 테스트 파일 모두 실제 import·assert 수행 (거짓 green 없음) |
| live 마커 | 정상 | tests 내 `mark.live` 없음 |
| ruff check · format | clean | 72 files formatted, 위반 0 |
| import-linter | 미설정 (M14) | grep 레이어 검사로 대체 → 위반 없음 (새 probe 는 `redteam.core` 만 import) |
| basename · 테스트 짝 | OK | 중복 basename 없음, 모든 모듈에 `test_<name>.py` |
| 비밀키 · Non-goal 의존성 | 없음 | 인라인 키 0, litellm/rich 미도입 |
| deptry | ⚪ 참고 (구간 밖) | jinja2(M12)·pytest/ruff/import-linter/deptry(dev그룹, M14) DEP002 — 모두 예상됨. openai/pyyaml 은 이제 사용되어 사라짐 |

## task 별 상세

### task 24 — M9 PAIR : ✅ PASS
- 완료 조건 대조
  - [x] 성공 시 중단 — `tests/test_pair_probe.py::test_pair_stops_on_success_k` (judge 2회째 성공 → 2라운드에 중단)
  - [x] target_calls==k — `::test_pair_stops_on_success_k` (`attempt.target_calls==2` **및** `counter.count==2` 동시 검증)
  - [x] attacker_calls==k — 같은 테스트 (`attempt.attacker_calls==2`, `len(attacker.calls)==2`)
  - [x] ≤B — `::test_pair_budget_exceeded_propagates` (budget 2 < max_attempts 5 → 3번째서 차단, `counter.count==2`)
  - [x] `"pair"` 등록 — `probes/__init__.py:25` `"pair": lambda: PairProbe()`; `available()["probe"]` 에 포함(런타임 확인)
  - [x] test_runner 확장 — `tests/test_runner.py::test_pair_e2e` (refusal→응낙 2라운드, counter 권위 `target_calls==2`)
  - [x] 예산초과 `BudgetExceeded` 전파(probe 삼키지 않음) — `::test_pair_budget_exceeded_propagates` + probe `pair_probe.py:68` 주석·미포획
  - [x] attacker 부재 시 `AdapterError` — `::test_pair_requires_attacker`, `::test_pair_binary_judge_graceful` (이진 judge N/A 렌더)
- 스펙 일치: 이름/필드/시그니처 plan 일치 (`name="pair"`, `uses_attacker/uses_judge=True`, `default_turns=1` — `::test_pair_probe_attrs`). Open questions 의 "graceful degradation"(이진 primary `score=None` 도 크래시 없이 N/A 렌더) 도 `::test_pair_binary_judge_graceful` 로 실증.
- 발견
  - ⚪ probe 가 `target_calls=len(turns)` 로 자가보고하나 runner 가 `counter.count` 로 override(`runner.py:126`) — StaticProbe 선례와 동일, 파이프라인에선 wrapper 가 권위이므로 Fair-ASR 원칙 위반 아님. 유닛테스트는 self-report 와 counter 를 둘 다 확인해 일치함.

### task 25 — M10 Crescendo + Manual 멀티턴 : ✅ PASS
- 완료 조건 대조
  - [x] conv 성장 — `tests/test_crescendo_probe.py::test_crescendo_conversation_grows` (매 chat 메시지 길이 1,3,5 + user/assistant 역할 교대 + 마지막이 새 user 발화)
  - [x] 턴별 judge — `::test_crescendo_judge_receives_conversation` (RecordingJudge 로 `conversation_lengths==[1,2]`)
  - [x] 성공 중단 — `::test_crescendo_stops_on_success` (judge 2회째 → 2턴 중단, `counter.count==2`)
  - [x] max_turns 중단 — `::test_crescendo_stops_at_max_turns` (실패만 → `target_calls==3==max_turns`)
  - [x] target_calls==turns — 위 두 테스트에서 확인
  - [x] `"crescendo"` 등록 — `probes/__init__.py:26`; `available()` 확인
  - [x] test_runner 에 ManualAdapter(scripted) 경유 Crescendo run — `tests/test_runner.py::test_crescendo_e2e_via_manual_adapter` (sentinel `.` 로 턴 종료, user 누적 ≥3줄 + assistant 등장으로 멀티턴 실증)
  - [x] runner 의 `BudgetExceeded` 흡수(budget<max_turns) — `::test_budget_exhausted_crescendo` (`budget_exhausted True`·`error is not None`·`turns==()`·`target_calls==2`)
- 스펙 일치: 이름/필드 일치(`name="crescendo"`, `default_turns=5` — `::test_crescendo_attrs`). attacker 가 누적 transcript 를 본다는 것도 `::test_crescendo_attacker_sees_conversation_context` 로 검증. attacker 부재 → `AdapterError`(`::test_crescendo_requires_attacker`).
- 발견
  - ⚪ 이 구간이 M0~M8 리포트에서 미검증으로 남겼던 "runner-level `BudgetExceeded`→`Attempt.error` 경로"(당시 정적 tier 1회 호출이라 도달 불가)를 `test_budget_exhausted_crescendo` 가 이제 커버 — 후속 보완 완료.

### task 26 — M11 Fair-ASR 마감 : ✅ PASS
- 완료 조건 대조
  - [x] 동일-B ASR — `tests/test_aggregate.py::test_fair_asr_three_tier_same_budget` (정적 0.5 / PAIR 1.0 / Crescendo 1.0)
  - [x] 평균 calls 열 — 같은 테스트 (`avg_target_calls` 정적1.0<PAIR4.0<Crescendo5.0) + `terminal_table.py:10` `avg_calls` 컬럼 존재
  - [x] efficiency 랭킹 — 같은 테스트 (efficiency 0.5/0.25/0.2, `ranked==[flip_attack,pair,crescendo]`) + `tests/test_terminal_table.py::test_three_tier_rows_ranked_by_asr` (표 행이 ASR desc 로 정렬)
- 스펙 일치: task 문구가 "test_aggregate/test_terminal_table **확장**" 이고, 구현 세션은 aggregate/terminal_table 로직이 M7 에서 이미 avg_target_calls·efficiency·ASR정렬을 갖췄다고 판단해 **테스트만 추가**했다. 이는 task 요구(테스트 확장)와 정확히 일치하며 plan 체크박스 주석에도 명시됨 → DEVIATION 아님.
- 발견
  - ⚪ M11 은 신규 구현 파일이 없다(순수 테스트 확장). aggregate 의 `efficiency=successes/total_calls`(`aggregate.py:83`), terminal_table ASR-desc 정렬(`terminal_table.py:36`)이 세 tier 를 올바르게 분리함을 확인.

## 계획과의 차이
- 없음. M9/M10 plan 체크박스에 추가된 문구(예산 전파·attacker부재 예외·runner budget 흡수 경로)는 모두 구현·테스트와 일치. M11 "테스트만 확장" 도 task 문구 그대로.
- ⚪ 참고(구간 밖): `examples/run.yaml` 이 기본 target 을 `manual` → `http_openai`(localhost Ollama `gpt-oss:20b`)로 변경. 실행 편의용 로컬 설정 변경으로 M9~M11 판정과 무관. `api_key_env: OLLAMA_API_KEY` 로 인라인 키 없음.

## 구현 세션 전달용 수정 요청
> M9~M11 은 수정할 🔴/🟡 없음. 아래는 위생 항목만.

```
M9~M11 검증 리포트(references/validation/2026-09-11-M9-M11-bd2ba8c.md): 구간 자체는 🔴/🟡 없이 PASS.
다만 이 구간(pair_probe/crescendo_probe + test 확장)이 아직 **미커밋(untracked/modified)** 상태다.
- 커밋해서 다음 검증이 working tree 가 아닌 커밋 기준으로 돌 수 있게 해줘.
- (선택) examples/run.yaml 의 target 이 로컬 Ollama 로 바뀐 게 의도면 그대로 두고,
  기본 예시를 오프라인 manual 로 유지하고 싶으면 되돌릴 것.
확인: `python -m pytest -q -rs` 에서 구간 모듈 skip 0 (현재 184 passed·skip 0 확인됨).
```
