# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Envs
.venv/
venv/

# Secrets — public repo: 타깃/judge API 키(Claude·OpenAI·Grok 등) 유출 방지
.env
.env.*
!.env.example
*.local.yaml
*.local.yml

# Claude Code 개인 설정 (공유용 .claude/settings.json 은 커밋)
.claude/settings.local.json

# Test / tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.import_linter_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# Project outputs (폴더는 유지, 내용만 무시) — 모델의 유해 응답이 담길 수 있음
/out/*
!/out/.gitkeep

# 로컬 모델 가중치 (대용량)
*.gguf
*.safetensors

# OS / editor
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/
